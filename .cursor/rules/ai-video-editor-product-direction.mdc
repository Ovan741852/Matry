---
description: AI video editor product direction and domain invariants
alwaysApply: true
---

# AI 影片編輯器產品方向

這個專案的核心是「storyboard-first 的 AI 影片生成編輯器」，不是傳統 timeline-first 剪輯器。

主要手感應像膠卷或分鏡板：使用者快速看到整個故事流程，一格一格 shot 設定秒數、分鏡圖、生成模式與結果。

## 不可破壞的產品規則

- 一支影片主要呈現為 ordered shots / film strip。
- 每格 shot 都可以設定秒數、註解、prompt、分鏡圖、生成模式與選用影片。
- 總片長由所有 shots 加總。
- 可在底層保留分組或父子結構，但 MVP UI 不應要求使用者理解「父子片段」。
- 最終影片由所有 shots 依順序組合。
- 初期合成只考慮影像，不處理音訊、字幕、轉場或配樂，除非使用者明確改 scope。

## 片段能力

每個 shot 可以有：

- 使用者看的標題與註解。
- 給 AI 使用的 prompt。
- 分鏡圖或參考圖。
- AI 生成影片候選版本。
- 使用者上傳或選擇的影片。
- 一個被選為最終組合使用的影片候選。
- 生成模式：text-to-video、first frame to last frame、reference to video。

## AI provider 原則

應設計成可替換 provider，而不是綁死單一 AI 影片服務。

- API key 與 provider 設定由使用者配置。
- Provider 能力差異要被建模，例如 text-to-video、image-to-video、可用時長、比例、模型名稱。
- 呼叫外部 AI 服務應集中在 adapter/service 邊界，不要散落在 UI 或資料模型裡。

## UI 原則

- 第一視角是分鏡膠卷與故事流程。
- Timeline 或 assembly view 是輔助，用於檢查 shot 順序與輸出準備狀態。
- 使用者不應需要懂 provider 技術名詞才能完成基本流程。
- 生成模式應用簡單語言呈現，例如「只用文字」、「起點到終點」、「參考圖生成」，避免讓使用者先理解模型能力表。
- 人看的文案用繁中、短句、能知道下一步；AI prompt 與機器處理資料可使用英文與結構化資料。

## 規格來源

產品規格草案見 `docs/ai-video-editor-prd.md`。若實作與此 rule 衝突，先更新規格或向 main agent/使用者確認。
